apiVersion: v1
kind: Service
metadata:
  name: {{ include "csv-uploader.fullname" . }}
  labels:
    {{- include "csv-uploader.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "csv-uploader.selectorLabels" . | nindent 4 }}
